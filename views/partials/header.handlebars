<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Scraper News</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/saved">Saved Articles</a>
            </li>
            <li class="nav-item">
                <button id="scrape" type="button" class="btn btn-primary">Scrape New Articles!</button>
            </li>
            {{#if articles}}
            <li class="nav-item">
                <button id="deleteAll" type="button" class="btn btn-danger">Clear Articles!</button>
            </li>
            {{/if}}
        </ul>
    </div>
</nav>
<div class="jumbotron text-center">
    <h1 class="display-4">{{ title }}</h1>
    <p class="lead">From NPR to you!</p>
</div>